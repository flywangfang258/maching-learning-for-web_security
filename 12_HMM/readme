HMM(Hidden Markov Model):隐马尔科夫模型算法特别适合处理时序数据，善于挖掘时序数据前后的关系，常应用于语音识别和自然语言处理领域。
在网络安全领域也广泛存在时序数据，如网站的访问顺序、系统调用顺序、管理员的操作命令等，都适合这种算法。

基于不同的特征提取方式使用隐马尔科夫算法来识别xss攻击及使用HMM来识别DGA域名。

HMM基础假设：一个连续的时间序列事件，它的状态由且仅由它前面的N个事件决定，对应的时间序列可以成为N阶马尔科夫链。
常见的基于GET请求的XSS\SQL注入、RCE(remote code execution)，其攻击载荷主要集中在请求参数中。